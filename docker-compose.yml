version: '3.8'

services:
  java-crm:
    image: java-crm:latest
    container_name: java-crm-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m -Xms256m -XX:+UseContainerSupport
      - TZ=UTC
      - DB_HOST=3.227.166.251
      - DB_NAME=U07k1T
      - DB_USER=U07k1T
      - DB_PASSWORD=53689053296
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge